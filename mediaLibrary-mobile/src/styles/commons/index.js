import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    backgroundImage:{
        width:360,
        height:700,
        resizeMode:'contain',
    },
    container: {
        flex: 1,
        backgroundColor: "#EAEAEC"
    },
});

