import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    header:{
        marginTop: 30,
        fontSize: 20,
        color: '#9e9e9e',
    },
    nextHeader:{
        marginTop: 30,
        fontSize: 22,
        marginLeft: 10,
        fontWeight: "bold",
        color: '#1976d2',
    }
});